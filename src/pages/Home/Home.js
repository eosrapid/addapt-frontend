import React from 'react';
import HomeWrapper from './Home.style';
import NavBar from '@/components/NavBar';

export default (props)=>{
  return(
      <HomeWrapper>
        <div className="newsFeedTitle"></div>
      </HomeWrapper>
  );
}